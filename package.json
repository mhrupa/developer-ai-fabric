{
  "name": "developer-ai-fabric",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:frontend": "npm --prefix frontend run build",
    "build": "npm run build:frontend && cd backend && mvn test",
    "serve": "cd backend && DEBUG=false mvn spring-boot:run",
    "test": "cd backend && mvn test"
  },
  "engines": {
    "node": ">=20"
  }
}
